ws.trigger("buildings.create", {
  result: "<%= @result %>",
  <% if defined? @intersection_pos%>
    intersection_pos: <%= @intersection_pos %>,
    building_type: "<%= @building_type %>"
  <% end %>
});