<h1>Login</h1>



<div>
	<%= form_for(:session, url:login_path) do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name  %>
		<%= f.submit "Login"%>
	<% end %>
</div>


<div>
  <%= form_for(:session, url:logout_path, method: :delete) do |f| %>
      <%= f.submit "Logout"%>
  <% end %>
</div>
